apply from: "$rootProject.projectDir/bs-library-non-core.gradle"

dependencies {
    // Kotlin
    implementation "org.jetbrains.kotlin:kotlin-reflect:$kotlin_version"

    // AppCompat
    implementation "com.android.support:appcompat-v7:$android_version_support"
}